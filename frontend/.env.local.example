# Frontend dev environment. Copy to .env.local and adjust as needed.
# In production deploys, .env.production is committed and used at build time.
# Local dev: this file overrides .env.production for `npm run dev`.
NEXT_PUBLIC_API_URL=http://localhost:4000/api
